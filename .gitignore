node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# Unity WebGL builds are big — keep them out of git unless you use LFS.
public/unity/Build/

# Headless captures from tools/shot.mjs — working files for art passes.
tools/shots/
