/**
 * The campsite ships one graphics preset on every device.
 *
 * One rendered pixel per CSS pixel is the approved visual baseline. It is not
 * adjusted from devicePixelRatio or frame-rate feedback.
 */
export const GRAPHICS_DPR = 1
